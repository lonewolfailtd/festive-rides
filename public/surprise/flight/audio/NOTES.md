# Book 2 — narration audio (`flight`)

Landing spot for the **audio agent**. See the brief and the rules first:
- `memory/juniper-storybook.md` — voice (Kylee NZ, `pcKdPWtbF6bM9o7NHjCI`),
  settings, Māori pronunciation map, speed pick (1.10 vs 1.18 TBD).
- `app/surprise/CLAUDE.md` — stay in your lane.

## What goes here
- `<id>.mp3` — one narration clip per frame (`1A.mp3` … `5C.mp3`), matching the
  paths already referenced in `app/surprise/flight/story.ts`.
- `<id>.json` — ElevenLabs word/char timestamps per clip (for subtitle sync).

## Lane
Only write files in this folder. Don't edit `story.ts`, components, the registry,
or any other book. Need a code change? Add it to this file as a request below.

## Requests for the book-2 (code) agent
- _(none yet)_
