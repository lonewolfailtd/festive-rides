import ChecklistClient from "./ChecklistClient";

export const metadata = {
  title: "Completion Checklist · Uni Citation Tool",
};

export default function ChecklistPage() {
  return <ChecklistClient />;
}
